import 'package:flutter/material.dart';

class WishalistPAge extends StatelessWidget {
  const WishalistPAge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Wishaist Page'),),
    );
  }
}